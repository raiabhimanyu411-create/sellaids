// utils/fixCloudinaryUrl.js - TEMPORARILY COMMENT OUT
export const fixCloudinaryUrl = (url) => {
  // TEMPORARILY DISABLED - returning original URL
  return url;
  
  // if (url && url.includes('cloudinary.com') && url.includes('/upload/')) {
  //   return url.replace('/upload/', '/upload/angle_90/');
  // }
  // return url;
};